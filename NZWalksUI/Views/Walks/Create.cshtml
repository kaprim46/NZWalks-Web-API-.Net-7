@model AddWalksViewModel
@{
    ViewData["Title"] = "Create";
}

<h1 class="mt-3">Create Walks</h1>
<form method="post">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <div class="mt-3">
        <label class="form-label">Name</label>
        <input asp-for="walksCreateDTO.Name" class="form-control border-0 shadow" />
        <span asp-validation-for="walksCreateDTO.Name" class="text-danger"></span>
    </div>

    <div class="mt-3">
        <label class="form-label">Length In Km</label>
        <input asp-for="walksCreateDTO.LengthInKm" class="form-control border-0 shadow" />
        <span asp-validation-for="walksCreateDTO.LengthInKm" class="text-danger"></span>
    </div>

    <div class="mt-3">
        <label class="form-label">Image Url</label>
        <input asp-for="walksCreateDTO.WalkImage" class="form-control border-0 shadow" />
    </div>

    <div class="mt-3">
        <label class="form-label">Description</label>
        <textarea asp-for="walksCreateDTO.Description" class="form-control border-0 shadow"></textarea>
        <span asp-validation-for="walksCreateDTO.Description" class="text-danger"></span>
    </div>

    <div class="mt-3">
        <label class="form-label">Regions</label>
        <select asp-for="walksCreateDTO.RegionId" asp-items="@Model.RegionsList" class="form-select border-0 shadow">
            <option disabled selected>--Select Region--</option>
        </select>
        <span asp-validation-for="walksCreateDTO.RegionId" class="text-danger"></span>
    </div>

    <div class="mt-3">
        <label class="form-label">Difficulties</label>
        <select asp-for="walksCreateDTO.DifficultyId" asp-items="@Model.DifficultiesList" class="form-select border-0 shadow">
            <option disabled selected>--Select Difficulty--</option>
        </select>
        <span asp-validation-for="walksCreateDTO.DifficultyId" class="text-danger"></span>
    </div>
    <div class="mt-3">
        <button type="submit" class="btn btn-success">Save</button>
        <a asp-controller="Walks" asp-action="Index" class="btn btn-primary">Back To List</a>
    </div>
</form>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
